[project]
name = "job-research-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=3.10.0",
    "python-dotenv>=1.2.3",
    "tavily-python>=0.8.1",
]

[project.scripts]
job-research-agent = "job_research_agent:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
